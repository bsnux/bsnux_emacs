;; -*- mode: Lisp; -*-

(setq prj-version "0.4")

(setq prj-list
      '(("eproject" "/home/arturo/.emacs.d/elpa/eproject-0.4")))

;(setq prj-last-open "eproject")

(setq prj-frame-pos
      '((top . 25)
        (left . 0)
        (width . 99)
        (height . 53)))

