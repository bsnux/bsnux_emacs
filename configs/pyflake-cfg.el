;; Cheking Python code
;;----------------------
;; Installing a checker, for example:
;;     $ pip install flake8
;; While visiting a Python file, run:
;;     M-x python-check or
;;     C-c C-v or
;;     C-c C-w
(setq python-check-command "flake8")
